{
  "name": "wasm-pqc-subtle-workspace",
  "private": true,
  "scripts": {
    "postinstall": "git rev-parse --git-dir >/dev/null 2>&1 && git config core.hooksPath .githooks || true",
    "test": "cargo test --all-features",
    "format": "cargo fmt --all"
  }
}
